{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "chmod -R 775 storage bootstrap/cache && php artisan config:cache && php artisan route:cache && php artisan view:cache && caddy run --config /etc/caddy/Caddyfile",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}